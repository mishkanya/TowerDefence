using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PointCointenier : MonoBehaviour
{
    public Transform[] Points;
    void Update()
    {
        if(Input.GetKeyDown(KeyCode.Space))
            Debug.Log(GetPoints._GetPoints.Length);
    }
}
